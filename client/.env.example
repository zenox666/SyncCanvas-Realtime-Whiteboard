# URL of the SyncCanvas realtime server.
# Defaults to http://localhost:3001 when unset.
# Create React App only exposes variables prefixed with REACT_APP_.
REACT_APP_SERVER_URL=http://localhost:3001
