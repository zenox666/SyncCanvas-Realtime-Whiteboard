# Port the realtime server listens on.
PORT=3001

# Comma-separated list of origins allowed to connect.
# Defaults to "*" for local development; set this in production.
CORS_ORIGIN=http://localhost:3000
